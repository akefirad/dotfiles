# Generated by chezmoi from the switches resolved in ~/.config/chezmoi/chezmoi.yaml
# (re-run `chezmoi init` to re-resolve). Sourced by env.sh so login shells see the
# resolved flag even when the ambient environment passed something else.
# CLAWBOT is a boolean flag: "1" = agent box, "" = not.
# GUI is intentionally NOT here — it's detected live in env.sh (see below).
export CLAWBOT={{ .clawbot | quote }}
